import 'package:flutter/widgets.dart';
import 'package:sos_vc/app/ui/layout.dart';

class NewPassPage extends StatelessWidget {
  static String tag = '/new-pass';

  @override
  Widget build(BuildContext context) {
    return Layout.render(
      // fab: null,
      content: Column(),
    );
  }
}
